# AGENTS.md

## 项目验证要求
本项目为零构建、少依赖的 Node 项目,暂不引入测试框架与 linter。任何代码变更提交前必须通过以下本地检查:

1. `node --check` 通过所有改动的 `.js` 文件(语法校验)。
2. `npm start` 能正常启动,无报错日志。
3. 对改动涉及的 API 用 `curl` 做冒烟验证(登录、发布、读取至少各一次)。
4. 前端改动需在浏览器实际打开页面确认渲染与交互正常。

临时测试脚本在验证完成后必须删除,不得残留在仓库中。

## 技术约束
- 依赖只允许 `hono` 与 `@hono/node-server`,新增依赖前必须先与用户确认。
- SQLite 一律使用 Node 内置 `node:sqlite`,禁止引入 better-sqlite3 等原生依赖。
- 所有持久化数据(数据库、上传文件)只能写入 `data/` 目录。
- 前端保持无构建步骤,第三方库以本地 vendor 文件引入。
