# 依赖
node_modules/

# 运行数据(SQLite + 上传图片,Docker 卷挂载点)
data/

# 定时备份输出(tools/backup.mjs)
backups/

TODO

# 系统与编辑器
.DS_Store
Thumbs.db
*.log

# 环境变量备份
.env
.agentdocs

# 代理工具配置与项目 skill,只留在本地,不进仓库
.claude/
.agents/

# 部署机本地复制模板后生成,不得被上传覆盖
docker-compose.yml

# 本地临时目录：参考素材(动画/视频片段)、验证输出，都不进仓库
tmp/

# 本地临时验证输出
work/
